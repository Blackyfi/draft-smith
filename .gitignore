# Node / frontend
node_modules/
dist/
dist-ssr/
*.local
.vite/

# Rust / Tauri
/src-tauri/target/
target/

# Signing material — must never be committed (keys live in ~/.tauri and CI secrets)
*.pfx
*.key
*.key.pub

# Claude local overrides (personal, not team-wide)
.claude/settings.local.json

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
